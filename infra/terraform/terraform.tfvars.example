environment  = "dev"
aws_region   = "us-east-1"
aws_profile  = "amiochat-personal"   # your personal AWS CLI profile — see infra/README.md
project_name = "amiochat"

# Optional: CloudWatch alarm emails (confirm SNS subscription after terraform apply)
# alarm_emails = ["you@example.com"]
